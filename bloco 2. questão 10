#include <iostream>
using namespace std;

float maior(float a, float b) {
    return (a > b) ? a : b;
}

int main() {
    float x, y;

    cout << "Digite dois números reais: ";
    cin >> x >> y;

    cout << "O maior número é: " << maior(x, y) << endl;

    return 0;
}
